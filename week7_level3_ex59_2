#include <stdio.h>  
#include <string.h>  
int main()  
{  
    char str[40]; // declare the size of character string  

    scanf ("%s", str);  
       
    printf ("%s ", strrev(str));  
    return 0;  
}  
