// Online C compiler to run C program online
#include <stdio.h>

int main(){

    char *p, str[50];
    p = str;
    scanf("%s", str);
    
    while(*p != '\0'){
        p++;
    }
    
    while(*p <= str){
        printf("%c", *p);
        p --;
    }
    
    return 0;
}
